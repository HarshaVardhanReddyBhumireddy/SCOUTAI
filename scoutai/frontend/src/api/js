const BASE_URL = "https://scoutai-backend-dohj.onrender.com";

export const API = {
  PARSE_JD: `${BASE_URL}/parse-jd`,
  GET_CANDIDATES: `${BASE_URL}/candidates`,
  OUTREACH: `${BASE_URL}/outreach`,
};

export default BASE_URL;