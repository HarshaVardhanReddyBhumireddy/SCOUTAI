const BASE_URL = "https://scoutai-backend-dohj.onrender.com";

export const API = {
  PARSE_JD: `${BASE_URL}/api/parse-jd`,
  GET_CANDIDATES: `${BASE_URL}/api/candidates`,
  OUTREACH: `${BASE_URL}/api/simulate-outreach`,
};

export default BASE_URL;